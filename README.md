# Vynex
Vynex is a local search engine that fetches real-time results from multiple sources. It combines them into a single result set, applies simple ranking, and prioritizes selected links from a custom list.
